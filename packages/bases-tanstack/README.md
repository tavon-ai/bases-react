# @bases-react/tanstack

TanStack Table adapter for evaluated Bases results.

## APIs

- `createBaseColumnDefs(result)` converts `BaseEvaluationResult` columns to TanStack column definitions.
- `displayValue(value)` formats Bases values for display/sorting/search helpers.

Used by `@bases-react/react` table rendering.
